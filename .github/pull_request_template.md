## What does this PR do?

<!-- A short description of the change and why it's needed. -->

Closes #

## Checklist

- [ ] `npm run typecheck` and `npm test` pass
- [ ] `npm run build` passes (and `npm run check:pack` for packaging or export changes)
- [ ] New behaviour has tests (jsQR round trip / `qrcode` comparison for encoder changes)
- [ ] README updated for API, CLI or limit changes
- [ ] `CHANGELOG.md` has an entry under `## [Unreleased]`
- [ ] Bundle size change noted below (`npm run size` before / after), if any

## Bundle size

<!-- Paste the relevant `npm run size` lines, or write "no change". -->
